# Model Weights

This folder contains the small trained weights used by the notebooks and web demo:

- `garment_classifier.pth`
- `compatibility_model.pth`

They are included so the project can be demonstrated without retraining. If the repository later grows past GitHub's normal file-size limits, move these weights to Git LFS or attach them to a GitHub Release.
